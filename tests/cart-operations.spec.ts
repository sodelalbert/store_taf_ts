import { test } from "../base/base.test.ts";

test.describe("Add to Cart", () => {
  test("Search Page Basic Operations", async ({ homePage }) => {
    await homePage.goto();
  });

  test("Cart Mutations", async ({ homePage }) => {
    await homePage.goto();
  });
});
